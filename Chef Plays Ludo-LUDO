#include <stdio.h>

int main(void) {
	// your code goes here
    int T,i,l;
    scanf("%d",&T);
    while(T--)
    {
        scanf("%d",&l);
        
        {
            if(l==6)
            printf("yes\n");
            else
           printf("no\n");
        }
    }
	return 0;
}
